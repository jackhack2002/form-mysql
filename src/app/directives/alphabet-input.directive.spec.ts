import { AlphabetInputDirective } from './alphabet-input.directive';

describe('AlphabetInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetInputDirective();
    expect(directive).toBeTruthy();
  });
});
